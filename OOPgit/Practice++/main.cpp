#include <bits/stdc++.h>
using namespace std;

// common datatypes
using ll = long long;
using ld = long double;
using db = double;

//pairs
using pi = pair<int,int>;
using pl = pair<ll,ll>;
using pd = pair<db,db>;

//vector ke different types
using vi = vector<int>;
using vb = vector<bool>;
using vl = vector<ll>;
using vd = vector<db>;

// maps
using mpi = map<int,int>;
using ompi = unordered_map<int,int>;
using omstr = map<string,string>;

// pairs
#define fi   first
#define se   second

// vectors
#define sz(x)    (int)(x).size()
#define all(x)   begin(x), end(x)
#define sor(x)   sort(all(x))
#define rsz      resize
#define ins      insert
#define ft       front()
#define bk       back()
#define pf       push_front
#define pb       push_back
#define lb       lower_bound
#define ub       upper_bound


const int MOD = 1e9+7;
const ld PI = 2*acos(0.0);


#define tempT template<typename T
tempT> T Swap(T a, T b){ T temp = a; a = b; b = temp; }



inline void Solve(){
    string str;
    cin>>str;
    string sub;
    cin>>sub;
}


int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int cases = 1; //cin>>cases;
    while(cases--){
	   Solve();
    }

    return 0;
}
